;;; init.el ---                       -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:

(when (maybe-require-package 'which-key) (which-key-mode))

(provide 'init-whichkey)
;;; init-whichkey.el ends here
